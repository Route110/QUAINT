<div class="container">
	<div class="row justify-content-center">
		<div class="col-lg-4">
		<h4 class="black_band">総合ランキング Top5</h4>
			<table class="table">
			<% @ranking.first(3).each_with_index do |ranking, i| %>
				<tr>
					<td><%= i+1 %>位</td>
					<td><%= ranking.hobby.name %></td>
					<td><%= link_to hobby_show_path(ranking.hobby.id) do %>
						<p class= "ranking-product-image"><%= attachment_image_tag ranking.hobby, :image, :fill, 200, 200, :size => "100x100", format: 'jpeg', fallback: "no-category-image.jpg" %></p>
						<% end %></td>
				</tr>
			<% end %>
			</table>
		<br>
		</div>
		<div class="col-lg-4">
		<h4 class="black_band">男性ランキング Top5</h4>
			<table class="table">
			<% @men.first(3).each_with_index do |men, i| %>
				<tr>
					<td><%= i+1 %>位</td>
					<td><%= men.hobby.name %></td>
					<td><%= link_to hobby_show_path(men.hobby.id) do %>
						<p class= "ranking-product-image"><%= attachment_image_tag men.hobby, :image, :fill, 200, 200, :size => "100x100", format: 'jpeg', fallback: "no-category-image.jpg" %></p>
						<% end %></td>
				</tr>
			<% end %>
			</table>
		</div>
		<div class="col-lg-4">
		<h4 class="black_band">女性ランキング Top5</h4>
			<table class="table">
			<% @women.first(3).each_with_index do |women, i| %>
				<tr>
					<td><%= i+1 %>位</td>
					<td><%= women.hobby.name %></td>
					<td><%= link_to hobby_show_path(women.hobby.id) do %>
						<p class= "ranking-product-image"><%= attachment_image_tag women.hobby, :image, :fill, 200, 200, :size => "100x100", format: 'jpeg', fallback: "no-category-image.jpg" %></p>
						<% end %></td>
				</tr>
			<% end %>
			</table>
		</div>
		<div class="col-lg-4">
		<h4 class="black_band">10〜30代ランキング Top5</h4>
			<table class="table">
			<% @young.first(3).each_with_index do |ranking, i| %>
				<tr>
					<td><%= i+1 %>位</td>
					<td><%= ranking.hobby.name %></td>
					<td><%= link_to hobby_show_path(ranking.hobby.id) do %>
						<p class= "ranking-product-image"><%= attachment_image_tag ranking.hobby, :image, :fill, 200, 200, :size => "100x100", format: 'jpeg', fallback: "no-category-image.jpg" %></p>
						<% end %></td>
				</tr>
			<% end %>
			</table>
		</div>
		<div class="col-lg-4">
		<h4 class="black_band">40〜60代ランキング Top5</h4>
			<table class="table">
			<% @middle.first(3).each_with_index do |ranking, i| %>
				<tr>
					<td><%= i+1 %>位</td>
					<td><%= ranking.hobby.name %></td>
					<td><%= link_to hobby_show_path(ranking.hobby.id) do %>
						<p class= "ranking-product-image"><%= attachment_image_tag ranking.hobby, :image, :fill, 200, 200, :size => "100x100", format: 'jpeg', fallback: "no-category-image.jpg" %></p>
						<% end %></td>
				</tr>
			<% end %>
			</table>
		</div>
		<div class="col-lg-4">
		<h4 class="black_band">70代〜ランキング Top5</h4>
			<table class="table">
			<% @older.first(3).each_with_index do |ranking, i| %>
				<tr>
					<td><%= i+1 %>位</td>
					<td><%= ranking.hobby.name %></td>
					<td><%= link_to hobby_show_path(ranking.hobby.id) do %>
						<p class= "ranking-product-image"><%= attachment_image_tag ranking.hobby, :image, :fill, 200, 200, :size => "100x100", format: 'jpeg', fallback: "no-category-image.jpg" %></p>
						<% end %></td>
				</tr>
			<% end %>
			</table>
		</div>
	</div>
</div>